import { Metadata } from 'next';
export const metadata: Metadata = {
  title: "How to Speak — Marzouq's Gaming Center",
  description: 'Master real-world communication skills. Job interviews, conflict resolution, public speaking, and more.',
};
export default function SpeakLayout({ children }: { children: React.ReactNode }) { return children; }
