import { Metadata } from 'next';
export const metadata: Metadata = {
  title: "Math Dog & Logic Cat — Marzouq's Gaming Center",
  description: 'Grade 5-12 math and logic challenges with Math Dog and Logic Cat. Adaptive difficulty.',
};
export default function MathDogLayout({ children }: { children: React.ReactNode }) { return children; }
