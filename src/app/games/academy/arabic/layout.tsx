import { Metadata } from 'next';
export const metadata: Metadata = {
  title: "Arabic — Marzouq's Gaming Center",
  description: 'Learn Arabic script, vocabulary, and phrases through interactive games.',
};
export default function ArabicLayout({ children }: { children: React.ReactNode }) { return children; }
